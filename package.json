{
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "serverless deploy"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.1",
    "serverless": "^3.38.0",
    "serverless-http": "^3.2.0",
    "stripe": "^14.5.0"
  }
}
